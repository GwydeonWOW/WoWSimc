// PostCSS config - Uncomment the tailwindcss line when running locally with npm install
// In production (Docker), the plain CSS is used during build
const config = {
  plugins: {
    // Uncomment this line after running npm install locally:
    // "@tailwindcss/postcss": {},
  },
};

export default config;
